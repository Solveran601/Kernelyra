# Kernelyra.Client

```csharp
using var engine = new Kernelyra.Client("./workspace");
var result = engine.Fit("train.csv", "label");
```

Install the Kernelyra engine first and ensure `kernelyra` is on `PATH`.
