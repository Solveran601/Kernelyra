# Third-party notices

Kernelyra source is Apache-2.0. Its required Python dependencies include NumPy
(BSD-3-Clause), FastAPI (MIT), Uvicorn (BSD-3-Clause), Pydantic (MIT) and
python-multipart (Apache-2.0). Optional integrations include TensorFlow
(Apache-2.0), PyArrow (Apache-2.0) and the MCP Python SDK (MIT).

This notice is informational and does not replace the license files distributed
by those packages. The release dependency manifest records the exact declared
ranges used to reproduce an artifact.
